# Calcular a diferença absolta entre dois números
primeiroNumero = float(input("Digite o primeiro número: "))
segundoNumero = float(input("Digite o segundo número: "))

resultado = abs(primeiroNumero-segundoNumero)

print(f"A diferença absoluta entre os números é: {round(resultado,2)}")